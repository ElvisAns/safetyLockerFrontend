import { defineStore } from "pinia";

export const useLogStore = defineStore("action_logs",{ //its good to start name all vue composable with use...
    state: () => {
        return {

        }
    }
})